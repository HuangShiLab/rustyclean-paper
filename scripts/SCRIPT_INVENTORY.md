# Script inventory

Which script produced which result. Generated by auditing `scripts/` against the
committed CSVs in `data/`.

**Totals:** 101 scripts — `main/` 59, `benchmark/` 19, `hpc/` 17, `minimal/` 6.
Of these, **21 scripts produced the 15 committed result files**; the rest are
infrastructure, superseded versions, or experiments whose output was never committed.

---

## A. Experiments with committed results

Each row is: run the benchmark → collect timings → compute accuracy → committed file.

### Exp 1–2 · RustyClean AUTO vs KneadData (full pipeline)

| Step | Script |
|---|---|
| Run | `benchmark/run_benchmark.sh` (SLURM: `rc_auto_vs_kneaddata`) |
| Collect | `benchmark/collect_results.py`, `benchmark/clean_metrics.py` |
| Accuracy | `benchmark/run_compute_accuracy.sh` → `benchmark/compute_accuracy_all.py` |

**Output** → `data/benchmark_results/auto_vs_kneaddata_metrics.csv` (runtime, memory,
routing decision) and `data/benchmark_results/accuracy_comparison.csv` (all three tools).

### Exp 3 · RustyClean `--skip-qc` vs Hostile (depletion only)

| Step | Script |
|---|---|
| Run | `benchmark/fair_hostile_skipqc_run_benchmark.sh` (SLURM: `rc_auto_skipqc_v2`) |
| Collect | `benchmark/fair_hostile_skipqc_collect_results.py` → `..._clean_metrics.py` |

**Output** → `data/benchmark_results/fair_hostile_skipqc_results.csv`
(raw: `rc_auto_skipqc_hostile_metrics_raw.csv`).
Related: `main/fair_hostile_fastp.sh` → `data/hostile_fastp_metrics.csv` (per-step fastp+Hostile timing).

### Exp 4 · Bowtie2 verification-pass ablation ⚠️ run from the `bowtie2-recheck` branch

| Step | Script |
|---|---|
| Baseline run | `benchmark/benchmark_baseline_kraken2_memmap.sh` |
| Recheck run | `benchmark/benchmark_bowtie2_recheck_v2.sh` (passes `--bowtie2-recheck`) |
| Baseline accuracy | `benchmark/run_accuracy_baseline_k2.sh` → `compute_accuracy_baseline_k2.py` |
| Recheck accuracy | `benchmark/run_accuracy_bowtie2_recheck_v2.sh` → `compute_accuracy_bowtie2_recheck_v2.py` |
| Timing repair | `benchmark/reparse_bowtie2_recheck_times.sh` |
| Compare / plot | `benchmark/compare_bowtie2_recheck.py` |

**Output** → `data/benchmark_results/{accuracy,performance}_baseline_kraken2_memmap.csv`
and `{accuracy,performance}_bowtie2_recheck.csv`. 3 replicates.

### Exp 5 · Depletion backend comparison

| Step | Script |
|---|---|
| Run | `main/benchmark_backend_runtime.sh`, `main/benchmark_rc_modes_kraken2_only.sh` |
| Plot | `main/plot_v4_backend_comparison.py` |

**Output** → `data/accuracy_rc_mm_bt_cf_v4.csv`, `data/performance_rc_mm_bt_cf_v4_corrected.csv`.

### Exp 6 · 100M matched panel (RustyClean sylph vs Hostile vs KneadData)

| Step | Script |
|---|---|
| Hostile run | `main/benchmark_100M_hostile.sh` |
| KneadData run | `main/benchmark_100M_kneaddata.sh` |
| Accuracy | `main/compute_accuracy_100M_matched_panel.py`, `main/compute_accuracy_100M_panel.py` |
| Accuracy wrappers | `main/run_accuracy_hostile_100M_v2.sh`, `main/run_accuracy_kneaddata_100M.sh` |
| Aggregate | `main/aggregate_matched_panel_n4.py` |

**Output** → `data/results_100M_matched/{accuracy,performance}.csv`.

### Exp 7 · 100M depletion-only matched panel (RustyClean + recheck vs Hostile)

| Step | Script |
|---|---|
| Run | `main/benchmark_rc_auto_skipqc_matched.sh`, `main/benchmark_rc_auto_skipqc_survey_matched.sh` |
| Accuracy | `main/run_accuracy_skipqc_matched.sh`, `main/run_accuracy_skipqc_survey_matched.sh` |
| Analyse | `main/analyze_accuracy_skipqc_matched.py` |

**Output** → `data/results_100M_skipqc_matched/{accuracy,performance}.csv`.

---

## B. Infrastructure (not experiments)

| Purpose | Scripts |
|---|---|
| Reference index builds | `main/prepare_grch38_t2t_fasta.sh`, `main/build_kraken2_t2t_only.sh`, `main/build_kraken2_grch38_t2t{,_v2,_v3,_v4}.sh`, `main/build_bowtie2_grch38_t2t{,_v2}.sh` |
| Simulated data generation | `main/generate_simulated_data.sh`, `main/generate_enhanced_data.sh`, `hpc/generate_data_slurm.sh`, `hpc/generate_data_sequential_slurm.sh` |
| Environment setup | `main/setup_env.sh`, `hpc/setup_hpc_env.sh`, `hpc/setup_slurm.sh`, `minimal/setup_minimal_env.sh` |
| SLURM orchestration | `hpc/submit_all.sh`, `hpc/run_benchmark_slurm.sh`, `hpc/run_benchmark_sequential_slurm.sh`, `hpc/run_single_benchmark.sh`, `hpc/analyze_slurm.sh`, `hpc/sync_to_hpc.sh`, `hpc/cleanup_results.sh` |
| Generic analysis | `main/analyze_accuracy.py`, `main/analyze_performance.py`, `main/integrate_results.py`, `main/run_post_processing.sh` |
| Reporting / figures | `main/generate_report.py`, `main/generate_final_report.py`, `main/plot_publication_figures_v2.py`, `main/plot_updated_paper_figures.py`, `main/make_status_deck.py` |
| Minimal validation path | `minimal/run_minimal.sh`, `minimal/generate_one_dataset.py`, `minimal/analyze_minimal.py`, `minimal/upgrade_to_standard.sh` |
| Housekeeping | `main/cleanup_rustyclean_failed.sh` |

---

## C. Experiments with scripts but **no committed results**

These are written and (in some cases) already run on the cluster, but nothing in
`data/` corresponds to them. Each is a gap the manuscript currently has.

| Experiment | Scripts | Why it matters |
|---|---|---|
| **AUTO decision boundary** | `main/benchmark_auto_decision_boundary.sh`, `main/plot_auto_decision_boundary.py` | **Highest value.** Forces both `bowtie2` and `kraken2` over a host ladder (0/1/5/10/30/100 %) at fixed 10M reads — this is the runtime-crossover evidence that justifies the default thresholds. Manuscript §2.3 asserts the thresholds follow measured behaviour but shows no such figure. |
| T2T-only index panels | `main/benchmark_t2t_only_matched_panel.sh`, `..._extended_panel.sh`, `..._pe_panel.sh`, `main/benchmark_rc_t2t_only_matched.sh`, `main/run_accuracy_t2t_only_{matched,panels}.sh`, `main/analyze_accuracy_t2t_only_panel.py`, `main/compare_t2t_only_panel.py` | The `_pe_panel` script is the **only paired-end experiment** in the repo. PE is currently unmeasured. |
| Cross-species hosts | `main/benchmark_cross_species.sh`, `main/analyze_accuracy_cross_species.py`, `hpc/benchmark_cross_species_v2.sh`, `hpc/generate_cross_species_data_v2.sh` | Mouse / rat / pig / rice / monkey indexes are built but no results are committed. |
| Real cohort | `main/benchmark_real_data.sh`, `main/real_data_downstream.sh`, `main/downstream_real_data.sh`, `hpc/benchmark_real_data.sh`, `main/rustyclean_LU.sh`, `main/submit_rustyclean_LU.sh` | All evaluation to date is simulated. |
| Downstream impact | `main/downstream_analysis.sh`, `hpc/downstream_slurm.sh` | Taxonomy / assembly / CheckM2 / diversity — the result reviewers will ask for. |

---

## D. Superseded / deprecated

`others/tmp_scripts/` — do not use: `tmp_benchmark_cross_species.sh`,
`tmp_generate_cross_species_data.sh`, `tmp_compare_accuracy.py`, `tmp_postprocess_all.sh`,
`tmp_real_data_validation.sh`, `compute_skipqc_accuracy.py`,
`plot_rc_hostile_kneaddata.py`, `update_manuscript_{docx,md}.py`.

Superseded but still in `scripts/main/`: `plot_publication_figures.py` (→ `_v2`),
`generate_publication_figures_v2.py` (→ `plot_updated_paper_figures.py`),
`build_kraken2_grch38_t2t{,_v2,_v3}.sh` (→ `_v4` / `build_kraken2_t2t_only.sh`),
`build_bowtie2_grch38_t2t.sh` (→ `_v2`), `compute_accuracy_100M_panel.py`
(→ `_matched_panel.py`).

---

## E. Reproducibility notes

1. **Exp 4 and Exp 7 were produced by the `bowtie2-recheck` branch, before it was
   merged.** That branch's `choose_auto_backend()` took three arguments (host %,
   low, high); `main` now takes five, having restored the read-count condition.
   Every dataset in those experiments exceeds the 20M-read threshold, so the
   backend selected is unchanged, but the rule that produced the results is not
   byte-for-byte the rule `main` implements.
2. `--bowtie2-recheck` now takes the Bowtie2 index prefix as its value: supplying
   the index enables the verification pass, omitting the flag disables it, and
   `--no-bowtie2-recheck` no longer exists. Scripts written against the old bare
   flag must pass an index. The exit-status fix for Kraken2 failures is on both
   `main` and `legacy-main-no-recheck`.
3. `data/results_100M_skipqc_matched/accuracy.csv` stores the Hostile rows with the
   **opposite positive class** to the RustyClean rows; metrics computed from that
   file without transposing are wrong.
4. `data/results_100M_matched/performance.csv` has KneadData and Hostile rows
   **missing the `rep` field**, so a naive `csv.DictReader` shifts their columns.
5. Hostile runtime does not reconcile between Exp 3 (60M/90 %: 71 min) and
   Exp 6 (100M/90 %: 22 min). One of the two runs used different settings.
